apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-baseline-chart.fullname" . }}
  labels:
    {{- include "eks-baseline-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-baseline-chart.selectorLabels" . | nindent 4 }}
